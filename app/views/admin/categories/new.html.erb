<div class="row">
	<h2>New Category</h2>

	<%= form_for [:admin,@category] do |f| %>
		<%= render 'shared/error_messages', object: f.object %>
		<%= render 'fields', f: f %>
		<%= f.submit "Create Category", class: "btn btn-large btn-primary new_category" %>
	<% end %>

</div>
