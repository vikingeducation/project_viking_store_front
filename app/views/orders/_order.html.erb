<tr>
  <td><%= order.id %></td>
  <td><%= link_to order.user.id, user_orders_path(order.user) %></td>
  <td><%= order.billing.nil? ? "N/A" : order.billing.street_address %></td>
  <td><%= order.billing.nil? ? "N/A" : order.billing.city.name %></td>
  <td><%= order.billing.nil? ? "N/A" : order.billing.state.name %></td>
  <td><%= order.purchases.sum(:quantity) %></td>
  <td>???</td>
  <td><%= order.checked_out ? "PLACED" : "UNPLACED" %></td>
  <td><%= order.checkout_date %></td>
  <td><%= link_to "show", user_order_path(order.user_id, order.id) %></td>
  <td><%= link_to "edit", edit_user_order_path(order.user_id, order.id) %></td>
  <td><%= link_to "delete", user_order_path(order.user_id, order.id), :method => :delete, :data => { confirm: "Are you sure?" } %></td>
</tr>